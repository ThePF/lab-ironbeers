<div class="beercard">
    <div class="beerPartialImage">
        <img src={{this.image_url}} alt={{this.name}} />
    </div>
    <div class="beerPartialText">
        <h1>{{this.name}}</h1>
        <h2>{{this.tagline}}</h2>
        <p>{{this.description}}</p>
    </div>
</div>